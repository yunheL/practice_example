# Cast a sub struct to a super struct

Topics:  
1. `struct a` is a member of `struct b`. What will happen if we cast `struct a*` to `struct b*`?
  
Usage:  
`make`  
`./cast`  

Run `make clean` to remove `cast` as needed.  
